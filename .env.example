PORT=3000
PUBLIC_BASE_URL=http://localhost:3000

# Leave empty to use the local JSON fallback in data/indicacoes.json.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_INDICACOES_TABLE=indicacoes

# Required for admin endpoints in production.
ADMIN_TOKEN=troque-esse-token

# Comma-separated list. Use * only while developing.
CORS_ORIGIN=http://localhost:3000
